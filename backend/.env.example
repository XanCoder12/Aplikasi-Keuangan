DB_HOST=localhost
DB_PORT=5432
DB_USER=finance
DB_PASSWORD=finance123
DB_NAME=finance
DB_SSLMODE=disable

SERVER_PORT=8080

# Google OAuth Client ID (Web application type, from Google Cloud Console)
GOOGLE_CLIENT_ID=

# JWT secret for signing app tokens (optional, defaults to a built-in secret)
JWT_SECRET=
